*** Jeekay: This should be done before next tag
When moving to a production network:

/Done Change query in checkUser() to include autokill again
/Done Readd return code in checkUser() to include autokill